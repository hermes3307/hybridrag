# Core dependencies
python-docx>=1.0.0
PyPDF2>=3.0.0
chardet>=5.0.0

# NLP libraries
spacy>=3.7.0
# After installing, run: python -m spacy download en_core_web_sm

# Neo4j connection
neo4j>=5.0.0

# LLM support
torch>=2.0.0
transformers>=4.30.0

# Anthropic API
anthropic>=0.2.0

# Terminal UI
rich>=13.0.0

# Document processing alternatives to textract
docx2txt>=0.8
pdfminer.six>=20200720

# Optional: If you want to use textract (has dependency issues)
# textract>=1.6.3
# Instead of textract, we'll use alternative libraries for each file type

# Additional utilities
tqdm>=4.66.0
requests>=2.31.0
pyyaml>=6.0.0